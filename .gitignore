# Compiled MQL5 binaries — regenerated from source, never commit
*.ex5
*.ex4

# Compile logs — regenerated on every build
compile.log

# MetaEditor auto-forward cache (legacy, kept for safety)
.mql-auto-forwards/

# clangd background symbol index — machine-specific, large, regenerated automatically
.cache/

# clangd compile database — contains machine-specific absolute paths
compile_commands.json

# VS Code workspace-local junk
.vscode/*.log

# OS junk
Thumbs.db
.DS_Store